const getBooksGroups = async (major) => [];
const getChapters = (bookID) => [];
const getSubjects = (cahpterID) => [];
const getLearningTests = (subjectID) => [];

const setGroup = (group) => {};
const setBook = (book) => {};
const setChapter = (chapter) => {};
const setSubject = (subject) => {};
const setLearningTest = (test) => {};
const setHint = (hint) => {};

const learningDbReady = async () => false;

const setLearningTestsData = async (data) => {};

export default setLearningTestsData;
export { learningDbReady, getBooksGroups };
